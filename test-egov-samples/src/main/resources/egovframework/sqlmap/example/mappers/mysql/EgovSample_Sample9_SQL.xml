<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample9.service.impl.SampleMapper">

	<resultMap id="sample9" type="egovframework.example.sample9.service.SampleVO">
		<result property="id" column="id"/>
		<result property="name" column="name"/>
		<result property="description" column="description"/>
		<result property="useYn" column="use_yn"/>
		<result property="regUser" column="reg_user"/>
	</resultMap>
	
	
	<select id="selectSampleIds" resultType="String">

			SELECT  'SAMPLE-'  || LPAD(SQ_IDS9.NEXTVAL,11,'0')  AS IDS FROM DUAL

	</select>
	

	<insert id="insertSample" parameterType="SampleVO">

			INSERT INTO SAMPLE9
				( ID
				  , NAME
				  , DESCRIPTION
				  , USE_YN
				  , REG_USER )
			VALUES ( #{id}
				  , #{name}
				  , #{description}
				  , #{useYn}
				  , #{regUser} )

	</insert>

	<update id="updateSample">

			UPDATE SAMPLE9
			SET ID=#{id}
				, NAME=#{name}
				, DESCRIPTION=#{description}
				, USE_YN=#{useYn}
				  WHERE ID=#{id}

	</update>

	<delete id="deleteSample">

			DELETE FROM SAMPLE9
			WHERE ID=#{id}

	</delete>

	<select id="selectSample" resultMap="sample9">

			SELECT
				ID, NAME, DESCRIPTION, USE_YN, REG_USER
			FROM SAMPLE9
			WHERE ID=#{id}

	</select>

	<select id="selectSampleList" parameterType="searchVO" resultType="egovMap">

			SELECT  *
			FROM  (
				SELECT ROWNUM RNUM, B.*
				FROM(
					SELECT
						 /*+ INDEX_DESC(SAMPLE9 SAMPLE9_PK) */  ID, NAME, DESCRIPTION, USE_YN, REG_USER
					FROM SAMPLE9
					WHERE 1=1
					<if test="searchKeyword != null and searchKeyword != ''">
				        <choose>
				            <when test="searchCondition == 0">
								AND	ID LIKE '%' || #{searchKeyword} || '%'
							</when>
				            <when test="searchCondition == 1">
								AND	NAME LIKE '%' || #{searchKeyword} || '%'
							</when>
						</choose>
					</if>
				) B WHERE ROWNUM <![CDATA[<=]]> #{firstIndex} + #{recordCountPerPage}
			)
			WHERE  RNUM  <![CDATA[>]]> #{firstIndex}
	</select>

	<select id="selectSampleListTotCnt" parameterType="searchVO" resultType="int">

			SELECT COUNT(*) totcnt
			FROM SAMPLE9
			WHERE 1=1
			<if test="searchKeyword != null and searchKeyword != ''">
		        <choose>
		            <when test="searchCondition == 0">
						AND ID LIKE '%' || #{searchKeyword} || '%'
					</when>
		            <when test="searchCondition == 1">
						AND	NAME LIKE '%' || #{searchKeyword} || '%'
					</when>
				</choose>
			</if>
	</select>

</mapper>